import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic_vue_test_app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
